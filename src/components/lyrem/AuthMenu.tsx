import{useEffect,useState}from"react";import{Link,useNavigate}from"@tanstack/react-router";import{LogIn,LogOut,Settings,User}from"lucide-react";import type{User as CloudUser}from"@supabase/supabase-js";import{Button}from"@/components/ui/button";import{DropdownMenu,DropdownMenuContent,DropdownMenuItem,DropdownMenuLabel,DropdownMenuSeparator,DropdownMenuTrigger}from"@/components/ui/dropdown-menu";import{supabase}from"@/integrations/supabase/client";import{t,type Locale}from"@/lib/lyrem-i18n";
export function AuthMenu({locale,onAccount}:{locale:Locale;onAccount:()=>void}){const[user,setUser]=useState<CloudUser|null>(null);const[ready,setReady]=useState(false);const navigate=useNavigate();useEffect(()=>{const{data:sub}=supabase.auth.onAuthStateChange((_e,s)=>setUser(s?.user??null));supabase.auth.getSession().then(({data})=>{setUser(data.session?.user??null);setReady(true)});return()=>sub.subscription.unsubscribe()},[]);if(!ready)return <div className="h-9 w-16" aria-hidden/>;if(!user)return <Button asChild variant="ghost" size="sm"><Link to="/auth" search={{mode:"login"}}><LogIn className="size-4"/>{t(locale,"login")}</Link></Button>;const name=(user.user_metadata?.["display_name"]as string|undefined)??(user.user_metadata?.["full_name"]as string|undefined)??user.email??"Account";const avatar=user.user_metadata?.["avatar_url"]as string|undefined;return <DropdownMenu><DropdownMenuTrigger asChild><Button variant="ghost" size="icon" aria-label={name}>{avatar?<img src={avatar} alt="" className="size-8 rounded-full object-cover"/>:<span className="grid size-8 place-items-center rounded-full bg-primary/15 text-primary"><User className="size-4"/></span>}</Button></DropdownMenuTrigger><DropdownMenuContent align="end" className="w-60"><DropdownMenuLabel className="truncate">{name}</DropdownMenuLabel><DropdownMenuSeparator/><DropdownMenuItem onClick={onAccount}><Settings className="size-4"/>{t(locale,"account")}</DropdownMenuItem><DropdownMenuItem onClick={async()=>{await supabase.auth.signOut();navigate({to:"/",replace:true})}}><LogOut className="size-4"/>{t(locale,"logout")}</DropdownMenuItem></DropdownMenuContent></DropdownMenu>}
