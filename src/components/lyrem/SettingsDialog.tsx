import{useEffect,useState}from"react";import{Gauge,Moon,Monitor,Settings2,Sun}from"lucide-react";import{Button}from"@/components/ui/button";import{Dialog,DialogContent,DialogDescription,DialogFooter,DialogHeader,DialogTitle}from"@/components/ui/dialog";import{Label}from"@/components/ui/label";import{Select,SelectContent,SelectItem,SelectTrigger,SelectValue}from"@/components/ui/select";import{Switch}from"@/components/ui/switch";import{localeLabels,t,type Locale}from"@/lib/lyra-i18n";import{type LyraSettings,type Theme}from"@/lib/lyra-store";import{cn}from"@/lib/utils";
type Props={open:boolean;onOpenChange:(open:boolean)=>void;settings:LyraSettings;onSave:(settings:LyraSettings)=>void};
const themes:[Theme,typeof Sun][]=[["dark",Moon],["light",Sun],["system",Monitor]];
export function SettingsDialog({open,onOpenChange,settings,onSave}:Props){const[draft,setDraft]=useState(settings);useEffect(()=>{if(open)setDraft(settings)},[open,settings]);const label=(id:Theme)=>t(draft.locale,id);return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-h-[90dvh] overflow-y-auto border-border bg-card sm:max-w-2xl"><DialogHeader><DialogTitle className="flex items-center gap-2"><Settings2 className="size-5 text-primary"/>{t(draft.locale,"settings")}</DialogTitle><DialogDescription>Lyra AI</DialogDescription></DialogHeader><div className="grid gap-7 py-3"><section className="space-y-3"><Label>{t(draft.locale,"appearance")}</Label><div className="grid grid-cols-3 gap-2">{themes.map(([id,Icon])=><Button key={id} type="button" variant={draft.theme===id?"secondary":"outline"} onClick={()=>setDraft({...draft,theme:id})} className={cn("h-auto flex-col gap-2 py-3",draft.theme===id&&"border-primary/50 bg-primary/10")}><Icon className="size-4"/>{label(id)}</Button>)}</div></section><section className="space-y-3"><Label htmlFor="locale">{t(draft.locale,"language")}</Label><Select value={draft.locale} onValueChange={value=>setDraft({...draft,locale:value as Locale})}><SelectTrigger id="locale"><SelectValue/></SelectTrigger><SelectContent>{(Object.entries(localeLabels)as[Locale,string][]).map(([id,name])=><SelectItem key={id} value={id}>{name}</SelectItem>)}</SelectContent></Select></section><section className="flex items-center justify-between gap-5 rounded-md border border-border p-4"><div><Label htmlFor="performance" className="flex items-center gap-2"><Gauge className="size-4 text-primary"/>{t(draft.locale,"performance")}</Label><p className="mt-1 text-sm text-muted-foreground">{t(draft.locale,"performanceHelp")}</p></div><Switch id="performance" checked={draft.performanceMode} onCheckedChange={checked=>setDraft({...draft,performanceMode:checked})}/></section></div><DialogFooter><Button variant="ghost" onClick={()=>onOpenChange(false)}>{t(draft.locale,"cancel")}</Button><Button onClick={()=>{onSave(draft);onOpenChange(false)}}>{t(draft.locale,"save")}</Button></DialogFooter></DialogContent></Dialog>}
